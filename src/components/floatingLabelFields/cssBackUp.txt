.float-label {
 position: relative;
 padding-top: 3.2px;

 .ant-input,
 .ant-picker {
  width: 100% !important;
  height: 40px;
  padding: 0.6rem 0.6rem 0.6rem 12px; // Add padding-left
  border: 1px solid #4d5e80;
  border-radius: 6px !important; // Add border-radius
  transition: border-color 0.2s ease, box-shadow 0.2s ease; // Add transition for box-shadow
  color: black;

  &:focus,
  &:hover {
   border-color: #7abfe3 !important;
   box-shadow: none !important;
  }
 }

 .ant-select {
  width: 100% !important;
  height: 40px;

  .ant-select-selector {
   box-shadow: none !important;
   border: 1px solid #4d5e80;
   border-radius: 6px !important; // Add border-radius
   transition: border-color 0.2s ease, box-shadow 0.2s ease; // Add transition for box-shadow
   color: black;
  }
  .ant-select-selector:hover,
  .ant-select-selector:focus-within {
   border: 1px solid #7abfe3 !important;
   box-shadow: none !important;
  }
 }
}

.label {
 position: absolute;
 pointer-events: none;
 left: 12px;
 top: 12px;
 transition: 0.2s ease all;
 color: #878787;
 font-size: 0.9rem;
}

.label-float {
 top: -6px;
 left: 12px;
 background: #fff; // Add background color to match the input's background
 padding: 0 4px; // Add padding to create space around the label
 font-size: 0.75rem;
}
